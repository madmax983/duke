1. **Analyze `generate_basic_block_cfg`**: The `generate_basic_block_cfg` function in `crates/duke-bytecode/src/cfg.rs` currently constructs a `HashMap` mapping block `start_pc` to `start_pc` to quickly test if a `next_block_id` exists. Since basic blocks are ordered sequentially (and `start_pc` increases monotonically), and `BasicBlock` slices are likely pre-sorted by `start_pc`, we can replace the `HashMap` with `slice::binary_search_by_key()`.
2. **Implement Binary Search**: We will modify `generate_basic_block_cfg` to drop the `HashMap` and use `blocks.binary_search_by_key(&next_block_id, |b| b.start_pc)` to perform the lookup. If it's successful, we know the `next_block_id` exists. Wait, let me verify if `blocks` are indeed sorted by `start_pc`.
3. **Check `build_basic_blocks`**: The blocks are generated by `build_basic_blocks` which uses a `BTreeSet` to find leaders and iterates over it, creating blocks in order of their `start_pc`. So the input is guaranteed to be sorted by `start_pc`.
4. **Update the code**: I will modify `crates/duke-bytecode/src/cfg.rs` to implement this optimization.
5. **Verify**: Run `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `cargo fmt --all`.
6. **Complete pre-commit steps**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
7. **Submit PR**: Submit the PR as Bolt.
