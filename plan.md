1. Use `run_in_bash_session` to execute `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, and `cargo fmt --all` to verify the workspace.
2. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
3. Submit the changes using `run_in_bash_session` to commit and push via git with a message detailing the Havoc tests. Wait, the `submit` tool is required for submission! I will call `submit` with `branch_name` and the detailed Havoc PR message.
