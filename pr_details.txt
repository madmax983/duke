🎯 Target: `crates/duke-interpreter/src/native.rs`
💣 Risk: Untested edge cases in `format_arg` with null objects, error extraction in atomic payload functions, and missing coverage on read/write lock native initialization can silently fail or yield bad strings/null.
🧪 Strategy: Added specific unit tests to exercise `AtomicPayload` invalid states, boolean formatting under different `%b` specs (including objects returning `true` and null returning `false`), and reentrant lock native state initialization tests.
🔬 Verification: Run `cargo test --package duke-interpreter --lib sentry_native_tests`
