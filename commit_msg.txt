🎻 Bard: [documentation update]

📖 Chapter: `duke` bin crate and `jar_diff` module.
🔦 Insight: The bin crate was completely undocumented, preventing `cargo doc` from running successfully without `--no-deps`. Fixed the types resolution bug in `jar_diff.rs`, and fully cleared out warnings.
🧪 Example: Clean build on `cargo doc`.
🖼️ Preview: All undocumented functions have been fully documented or safely allowed.
