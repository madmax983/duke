🧨 **The Trigger:** Input class name string with 20MB length passed to `ClassLoader::find_class` caused buffer overflow/OOM when formatting paths or errors.
📉 **The Stack Trace:** `memory allocation of 20971520 bytes failed \n process abort signal`
🧪 **Reproduction:** Run `cargo test --test havoc_directory_loader_oom`
😈 **Comment:** You assumed the `name` argument would never be larger than RAM. You were wrong. JVM specs cap class names at 65535 bytes, so I hard-capped them.
