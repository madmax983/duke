🧨 **The Trigger:** A JVM thread panics and poisons the CompletionRuntime Mutex.
📉 **The Stack Trace:** thread '...' panicked at 'called `Result::unwrap()` on an `Err` value: PoisonError ...'
🧪 **Reproduction:** Run the `havoc_thread_start_loom.rs` test with `cargo test`.
😈 **Comment:** You assumed the JVM workers would never panic. You were wrong.
