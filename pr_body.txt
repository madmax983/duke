🚮 Smell: The codebase contained numerous "Pyramids of Doom" consisting of deeply nested `match` and `if let` blocks across multiple crates, creating high cognitive load.
✨ Solution: Extracted the stack op block in `decoder.rs` to a helper function. Flattened nested logic across `cfg.rs`, `lib.rs`, `jar_diff.rs`, `instruction.rs` and `frame.rs` using `let else` guard clauses, early returns, and tuple matching.
🧼 Benefit: Significantly reduces nesting depth and improves readability while strictly enforcing Rust idiomatic patterns.
🛡️ Verification: Tests passed. No logic changed.
