**Tangle:** The `duke_classfile::types` module was removed from the public API, but `jar_diff.rs` still attempted to import it, causing build failures.
**Blueprint:** Updated `jar_diff.rs` to import types directly from `duke_classfile`, respecting the new boundaries.
**Stability:** Reduced coupling, faster compile times.
**Verification:** Builds successfully, strict separation enforced.
