# MEMORY

- 2026-04-29: Before issue #637, ServiceLoader providers whose static initialization or constructors touched `java.util.concurrent.atomic` failed before useful work. The `service-loader-atomic.jar` fixture now exercises a provider backed by `AtomicLong`; `AtomicServiceLoaderInteropTest.providerClinitUsesAtomicLong()` passes after the atomic synthetic classes and natives were added.
- 2026-04-30: Before issue #640, classes whose static initialization touched `java.util.concurrent.ConcurrentHashMap` could fail before `main()` because Duke had no CHM synthetic class or natives. `ConcurrentHashMapStaticInitTest` now covers a static cache that calls `put` and `computeIfAbsent` during `<clinit>` and then reads/prints `42 84`.
- 2026-04-30: Before issue #643, wire-protocol style static initialization that called `"text".getBytes(StandardCharsets.UTF_8)` or `Charset.forName("UTF-8")` failed because Duke had no `java.nio.charset` synthetics or `String` byte conversion natives. `CharsetClinitInteropTest` now covers a static UTF-8 header plus protocol charset lookup and prints `11` / `UTF-8`; the round-trip fixtures cover UTF-8, UTF-16, UTF-16BE, UTF-16LE, US-ASCII, and ISO-8859-1.
- 2026-04-30: Before issue #648, any class initializer caching `java.util.logging.Logger.getLogger(...)` failed before `main()` because Duke had no JUL synthetic classes. `JulClinitSurvivalTest` now covers the representative `Logger` + `AtomicLong` + `Pattern` static-init chain, and the JUL fixtures verify `INFO`/`WARNING` routing, `FINE` dropping, canonical `Level` statics/parse, logger interning, and `LogRecord` field storage.
- 2026-04-30: Before issue #649, config-heavy JARs failed on `new java.util.Properties()` because Duke had no synthetic `Properties` class. `PropertiesBasicTest` now covers ISO-8859-1 `load(InputStream)`, Java properties escapes, defaults chaining, deterministic `store(OutputStream, String)` round-trips, property-name enumeration, and local map views. `Properties` extends a minimal synthetic `Hashtable` for hierarchy compatibility, but Duke still does not model `Hashtable` synchronization.
- 2026-05-01: Before issue #658, libraries that touched richer `java.time` APIs would fall over on missing synthetic classes or identity-based semantics. `JavaTimeBasicTest` now covers `Instant`, `Duration`, `LocalDate`, and `LocalDateTime` constructors/factories, arithmetic, parse/toString round-trips, value-based `compareTo`/`equals`/`hashCode`, `Instant.now()` wall-clock interop, `Duration.between(...)`, and `DateTimeFormatter` ISO constants plus `DateTimeParseException` hierarchy wiring.
- 2026-05-01: Issue #660 added Duke's first hermetic third-party OSS JAR smoke harness under `tests/fixtures/oss-jars/`, using `slf4j-simple-2.0.13` + `slf4j-api-2.0.13` plus a `Slf4jSimpleSmoke` fixture. The harness initially discovered `java/lang/System.getSecurityManager()Ljava/lang/SecurityManager;` as the first missing capability in execution order, so the full end-to-end canary remains `#[ignore]` while the smoke advances one explicit gap at a time.
- 2026-05-12: Issue #695 added the no-security-manager `java/security/AccessController.doPrivileged(...)` slice, including `PrivilegedAction`, `PrivilegedExceptionAction`, and checked-exception wrapping through `PrivilegedActionException.getException()`. The `slf4j-simple` smoke now progresses past `System.getSecurityManager()` and `AccessController.doPrivileged(...)`; the current explicit blocker is `java/lang/Thread.getContextClassLoader()Ljava/lang/ClassLoader;`.
- 2026-06-08: Issue #854 registered `java/lang/String.equalsIgnoreCase(Ljava/lang/String;)Z` on the synthetic `java/lang/String` class, implementing Java SE 21's locale-independent two-step fold (`Character.toUpperCase` OR `Character.toLowerCase`) via `chars_equal_ignore_case` so corner cases like the KELVIN SIGN `U+212A` fold to `'k'`. Null argument returns false; differing-length strings short-circuit. New `StringEqualsIgnoreCase` fixture prints `OK`. The `slf4j-simple` smoke now progresses past `String.equalsIgnoreCase`; the current explicit blocker is `java/util/ArrayList.<init>(I)V`.
- 2026-07-10: Issue #1308 cleared the final `slf4j-simple` blocker chain and lit up the end-to-end canary. The last six explicit capabilities were added in execution order: `java/util/ArrayList.<init>(I)V`, `java/lang/StringBuilder.<init>(I)V`, `java/lang/Thread.getName()Ljava/lang/String;`, `java/lang/StringBuilder.append(Ljava/lang/CharSequence;II)Ljava/lang/StringBuilder;`, `java/lang/Class.isArray()Z`, and `java/io/PrintStream.flush()V`. `slf4j_simple_smoke_runs_real_jar_bytecode` is now un-ignored and passing: it runs unmodified `slf4j-simple` bytecode to completion, loading `org/slf4j/simple/SimpleLogger` and `org/slf4j/simple/SimpleServiceProvider` from classfiles (`ClassLoadSource::Classfile`) and emitting `INFO duke-smoke - hello world`. The now-obsolete `slf4j_simple_smoke_surfaces_next_missing_capability_explicitly` probe (which asserted an explicit next blocker via `expect_err`) was removed, since the smoke no longer returns an error.
- 2026-07-10: The `commons-lang3-3.17.0` end-to-end canary is now green. `commons_lang3_smoke_runs_real_jar_bytecode` runs unmodified `commons-lang3` bytecode to completion (`StringUtils.join`/`capitalize`, `ArrayUtils.add`/`contains`/`toObject`), emitting `commons-lang3: join=duke-jvm-smoke capitalize=Hello added=1,2,3,4 contains4=true`. The blocker chain was cleared in execution order: `java/lang/Character.toTitleCase(I)I`+`(C)C` (simple titlecase mapping; digraphs `U+01C4..U+01CC`/`U+01F1..U+01F3` map to their `Lt` forms, everything else to simple uppercase — validated against OpenJDK 21 goldens in a `tests_char_titlecase` unit test), `java/lang/Character.charCount(I)I`, `java/lang/String.<init>([III)V` (code-point-array constructor used by `StringUtils.capitalize`), `java/lang/reflect/Array.newInstance(Ljava/lang/Class;I)Ljava/lang/Object;`/`getLength(Ljava/lang/Object;)I`/`set(Ljava/lang/Object;ILjava/lang/Object;)V` (first `java.lang.reflect.Array` natives; `newInstance` maps primitive `TYPE` classes such as `Integer.TYPE` to `int[]` and `set` unboxes wrappers into primitive arrays), `java/lang/Class.getComponentType()Ljava/lang/Class;`, and `java/util/Arrays.setAll([Ljava/lang/Object;Ljava/util/function/IntFunction;)V` (element-wise lambda fill, used by `ArrayUtils.toObject`). A final rendering gap was also fixed: `java/util/Objects.toString(Object)`/`toString(Object,String)` were bypassing wrapper handling and printing `ClassName@ref`; they now delegate to `heap_object_to_string`, so boxed `Integer`s stringify as their values. The former next-blocker pin `commons_lang3_smoke_surfaces_next_missing_capability_explicitly` was repurposed into `commons_lang3_smoke_surfaces_no_missing_capability`, a regression guard that fails with the exact rendered capability if any blocker reappears.
- 2026-07-10: The `gson-2.11.0` `toJson`/`fromJson` canary (`oss_jar_smoke::gson_smoke_runs_real_jar_bytecode`) is now GREEN end-to-end: it runs unmodified `gson` bytecode to serialize `new Pojo(7, "duke")` and deserialize it back, printing `gson round-trip: {"count":7,"name":"duke"} -> count=7 name=duke`. The load-bearing root cause was a GC bug where nested `<clinit>` execution states were not registered as GC roots, so a collection during a nested class initializer freed live objects and left stale roots; fixed separately in PR #1318 / branch `swarm/gc-nested-clinit-roots` ("register all active nested execution states as GC roots"). On top of that fix, a 21-blocker capability chain was cleared in execution order: the `java/lang/reflect/Type` marker interface, the `Class` reflection natives (`getModifiers`, `isInterface`, `isPrimitive`, `isRecord`, `isAnonymousClass`, `getGenericSuperclass`, `isAssignableFrom`, `cast`), `Field.getModifiers`/`getGenericType`/`getType`/`get`, reflective `sun/misc/Unsafe.allocateInstance` for `fromJson` (gson's `ConstructorConstructor` fallback for `Pojo`), boxing all numerics (`Long`/`Short`/`Byte`/`Double`/`Float`, mirroring `Integer`) to extend `java/lang/Number` so `instanceof`/`checkcast Number` is correct, plus assorted `String`/`StringBuffer`/`Map` natives. KNOWN LIMITATIONS still open (tracked as `TODO(known-limitation)` in `native/reflect.rs`): `Class.getModifiers`/`isInterface` report ACC_PUBLIC-only (the real `ClassAccessFlags` are not carried on `ReflectedClassInfo`), and `Field.getModifiers` omits the transient/final bits (dropped when the Field mirror is built) — surfacing either needs plumbing through `registry.rs`. Additionally `sun/misc/Unsafe` and the synthetic `ThreadLocal` are not on the `KEEP_SYNTHETIC` allowlist, so an eventual `real_jdk_shadow` run would shadow them with real JDK bytecode that Duke does not yet fully support.
- 2026-07-11: System/IO real-layout phase 1 = investigation + `setOut0`/`setErr0`/`setIn0` System-init seeding scaffolding (commit `df33dcd`; shared `set_system_stream` helper + 3 static callback natives on `java/lang/System`, bootstrap routed through the same helper byte-identically, +5 tests). No `KEEP_SYNTHETIC` reduction was safely landable (stayed at 10) — full blocker map in `docs/findings/2026-07-11-system-io-real-layout-blockers.md`. KEY durable facts for future waves: (1) **bytecode wins over registered natives for shadowed classes** (`apply_shadow_override` `NativeOverride`→`Bytecode` at `execution.rs:40-46`, invokestatic path `execution.rs:542-566`, gate `registry.rs:1038-1041`), so real-layout migrations CANNOT use a native-shim shortcut — the real object graph must be genuinely well-formed. (2) Real instance-slot counts: `System` = 0 (slot-match but behavioral blocker), `PrintStream` = 8 own / 11 total (NOT "11 own"), `FileOutputStream`/`FileInputStream` = 5 each. (3) Dropping `File*Stream` is a VERIFIED REGRESSION: real `FileOutputStream.<clinit>@0` → `SharedSecrets.getJavaIOFileDescriptorAccess()` (`FD_ACCESS==null`) → `MethodHandles.Lookup.ensureInitialized` → the whole `java.lang.invoke` engine → unregistered `jdk/internal/reflect/Reflection.getClassAccessFlags(Ljava/lang/Class;)I` crash; also drags in `Blocker` (worse because Duke seeds `VM.isBooted()==true` at `jdk_internal.rs:231`) and `Cleaner`/`FileCleanable` — a Module/reflection lane (`Reflection.getClassAccessFlags` + `MethodHandles` bootstrap) is the SHARED prerequisite. (4) `System` migration is additionally gated by two tests that hard-lock it synthetic: `tests/real_jdk_shadow.rs` `KEEP_SYNTHETIC_SAMPLE` (~line 110) and `tests/layout_coherence.rs` `audit_reports_nonempty_candidates` (~line 213).
- 2026-07-11: Minimal-but-honest `java.lang.Module` model + real class/field access-flag plumbing. (1) Access flags are now threaded end to end through the reflection channel: `ReflectedClassInfo` and `ReflectedFieldInfo` carry a raw `access_flags: u16`, populated from the real classfile in `reflected_class_info_from_loader` (and reconstructed to the legacy default for synthetic-stub fallback paths). The three `TODO(known-limitation)` stubs in `native/reflect.rs` are DELETED and corrected: `Class.getModifiers` now returns the real class flags masked to the JLS set with `ACC_SUPER`/`ACC_MODULE` stripped (matching `HotSpot`'s `JVM_GetClassModifiers`), `Class.isInterface` reads the real `ACC_INTERFACE` bit, and `Field.getModifiers` reports the real field flags including `final` (0x10) / `transient` (0x80) / `volatile` (0x40) — previously public/static-only. All three became `register_callback` natives so they can read `ops.inspect_class(...)`. `ClassContext`/`FieldEntry` were intentionally NOT touched (239/272 literals) — the flags ride the `Reflected*Info` structs, which is the actual channel natives use. (2) `java.lang.Module`: every class lives in the shared unnamed module of the system class loader. New natives (in `native/java_lang.rs`, registered in `stdlib.rs`) intercept the real JDK methods #1319-style: `Class.getModule()` returns a lazily-allocated, string-interned singleton `java/lang/Module` instance (no synthetic class registered, no `KEEP_SYNTHETIC` entry needed — natives answer every call), `Module.isNamed()`→false, `Module.getName()`→null, `Module.canUse(Class)`→true; plus `jdk/internal/reflect/Reflection.getClassAccessFlags(Class)` returning the real class flags. Under `real_jdk_shadow` with a real JDK-21 jimage, the unmodified slf4j-simple smoke now flows the real `java.util.ServiceLoader` PAST the entire module-system wall — `Class.getModule` → `Module.isNamed` → `Reflection.verifyModuleAccess` → `getClassAccessFlags` → `ServiceLoader.checkCaller` returns — landing at the NEXT (separate, larger) frontier: `java/lang/ClassLoader.<clinit>` → `registerNatives()V` / the system-class-loader bootstrap (the same ClassLoader wall the spring-boot fixtures are pinned behind, out of scope here). Before this change the wall was `MethodNotFound java/lang/Class.getModule`. Proof: new in-crate unit tests exercise the four Module natives + the corrected `getModifiers`/`isInterface`/`Field.getModifiers` (transient|final|private surfaced), and `tests/module_model.rs` is a real-jdk-gated regression guard that the module wall stays cleared. The gson/commons-lang3/slf4j canaries remain green (the reflect natives still satisfy gson). Remaining KNOWN LIMITATION: `sun/misc/Unsafe` and synthetic `ThreadLocal` are still not on `KEEP_SYNTHETIC`.
- 2026-07-11: Broke the `java/lang/ClassLoader` bootstrap wall under `real_jdk_shadow` (branch `swarm/classloader-bootstrap`). Because `ClassLoader` is NOT on `KEEP_SYNTHETIC`, the real JDK-21 `ClassLoader` classfile loads and its real `<clinit>` runs; its very first instruction is a private static `registerNatives()V`, which is genuinely `native` in the JDK (no bytecode body). With no native registered, the interpreter ran off the empty method body and died with the opaque `FellOffEnd` error at `<clinit>@16` — the entire ClassLoader init walled at instruction 1. FIX: a single no-op native `java/lang/ClassLoader.registerNatives()V` (`native_class_loader_register_natives` in `native/java_lang.rs`, registered in the ClassLoader block of `stdlib.rs`; Duke wires each ClassLoader native explicitly so there is nothing to register). This is a minimal-native fix, NOT a `KEEP_SYNTHETIC` addition — the primary mission goal was to make the real `<clinit>` work, which it now does. WHAT NOW INITIALIZES: the real `ClassLoader.<clinit>` runs to completion (`@26 return`), and the real `getSystemClassLoader()` → `initSystemClassLoader()` path runs the real bytecode and returns the system loader (`getSystemClassLoader@95 areturn`). Execution then flows well past the ClassLoader lane into the real `ServiceLoader` module lookup: `ServiceLoader$ModuleServicesLookupIterator` → `jdk/internal/loader/BootLoader.<clinit>` → `SharedSecrets.getJavaLangAccess` → `jdk/internal/util/StaticProperty.<clinit>`. NEW VERBATIM FRONTIER: `MethodNotFound { name: "java/lang/System.getProperties", descriptor: "()Ljava/util/Properties;" }`. This is a SEPARATE subsystem (system-properties bootstrap), not the ClassLoader lane: `StaticProperty.<clinit>`'s first act is `System.getProperties()`, and under the shadow flag `java/util/Properties` is itself shadowed by real `Hashtable` bytecode, so satisfying it requires materializing a live, well-formed shadowed `Properties`/`Hashtable` object graph during bootstrap (recall the durable "bytecode wins over registered natives for shadowed classes" fact) — a cross-lane, non-minimal refactor, deliberately left as the honest pin. PROOF/GUARD: new non-ignored real-jdk-gated pin test `tests/classloader_bootstrap_frontier.rs::slf4j_real_jdk_shadow_classloader_frontier_pin` drives the same slf4j path and asserts this exact verbatim first-blocker; the pre-existing `module_model::slf4j_clears_module_wall_under_real_jdk_shadow` stays green (its assertions were not weakened). OBJECT-MODEL COHERENCE: the synthetic `native_class_loader_get_system_class_loader` (which caches into the `$dukeSystemClassLoader` static field) and the real `getSystemClassLoader` bytecode are mutually exclusive by mode — under the shadow flag the real bytecode wins (the synthetic ClassLoader ctx with its static field is shadowed away and never registered), so the two never diverge; nothing needed changing for coherence. UNRELATED (forbidden lane, NOT fixed, still reproduces): the real-jdk fat-jar path panics with `index out of bounds: the len is 1 but the index is 10` at `execution.rs:1614:46` (getfield) — verified still present via `DUKE_REAL_JDK=1 duke -jar tests/fixtures/oss-jars/spring-boot/duke-spring-boot-app-3.5.12.jar`. The two spring-boot synthetic pins (`spring_boot_real_app`: APP → `DateTimeFormatter.ofPattern`, LADDER → `MarkerManager`) did NOT move — they run in default synthetic mode, past the ClassLoader lane, so the registerNatives native (shadow-only) has no effect on them.
- 2026-07-12: Hashtable Map-default family + real `System.getProperties()` (branch `swarm/properties-hashtable`). TWO pins advanced. (1) LADDER (synthetic mode), registration-only: the synthetic `java/util/Hashtable` (which reuses the HashMap layout + `native_hashmap_*` natives, `stdlib.rs`) previously registered only init/put/get/containsKey/size/remove/isEmpty/keySet/values/entrySet/clear. It now registers the full Map-default family, mirroring HashMap: value-returning via `register` (`putIfAbsent`, `getOrDefault`, `replace`, `containsValue`, `putAll`) and lambda-driven via `register_callback` (`computeIfAbsent`, `computeIfPresent`, `compute`, `merge`, `forEach`, `replaceAll`) — all reusing the existing HashMap natives (no new native code). PAYOFF: the commons-logging ladder fixture's `LogFactoryImpl` fallback path clears the `Hashtable.computeIfAbsent` rung; the ladder frontier ADVANCED from `method not found: java/util/Hashtable.computeIfAbsent(Ljava/lang/Object;Ljava/util/function/Function;)Ljava/lang/Object;` to `method not found: org/apache/commons/logging/impl/LogFactoryImpl.objectId(Ljava/lang/Object;)Ljava/lang/String;` (a different missing-method rung in commons-logging's own bytecode; the collections lane is cleared). `LADDER_BLOCKER` in `duke/tests/spring_boot_real_app.rs` updated to the new frontier. (2) REAL-JDK (`DUKE_REAL_JDK=1`), new native: `System.getProperties()Ljava/util/Properties;` is now a `register_callback` native (`native_system_get_properties` in `native/java_lang.rs`, registered in the System block of `stdlib.rs`). Because under the shadow flag `java/util/Properties` is shadowed by real `Hashtable` bytecode (bytecode-wins rule), a native shim is impossible — so it materializes a GENUINE, real-layout Properties object graph, mirroring `native_reflect_constructor_new_instance`: `ops.ensure_class_initialized("java/util/Properties")` → `ops.allocate_instance("java/util/Properties")` → `ops.invoke("<init>","()V")` (runs REAL bytecode under shadow) → for each system property `ops.invoke("setProperty","(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/Object;")` with real heap Strings → return the ref. The key/value set is the `system_property_snapshot` helper, consistent with `system_property_value` (`common.rs`, the `java.version="21"` set — NOT the inconsistent `system_property_value_fallback` "1.8.0" set), plus any `setProperty` overrides. VERIFIED FULLY CONSTRUCTED (no half-migration): traced execution confirmed `ensure_class_initialized`, `allocate_instance`, real `<init>()V`, and all 9 `setProperty` invocations complete and the live Properties ref returns. The real-jdk frontier (`tests/classloader_bootstrap_frontier.rs`, `EXPECTED_FRONTIER`) ADVANCED from `MethodNotFound { name: "java/lang/System.getProperties", descriptor: "()Ljava/util/Properties;" }` to `InvalidFieldref { index: 0 }`. That new blocker is in a SEPARATE lane: `StaticProperty.<clinit>` reads the returned properties and execution proceeds until a real `getstatic java/lang/String.COMPACT_STRINGS` — a real-JDK static field that the synthetic, `KEEP_SYNTHETIC` `java/lang/String` does not declare, so `static_field_idx` fails with `InvalidFieldref { index: 0 }` (identified via temporary instrumentation, since reverted). This is the String real-layout / `KEEP_SYNTHETIC` lane, out of scope for the system-properties lane and left as the honest pin. COHERENCE: HelloWorld runs clean in synthetic and real-jdk modes; `DUKE_LAYOUT_CHECK=fail DUKE_REAL_JDK=1` HelloWorld shows no layout-check failure; the returned Properties is fully real-layout (allocate + real `<init>`), so no synthetic/real slot divergence. Full workspace suite stays 3083/0/4; clippy pedantic+nursery (1.97.0) and fmt clean; slf4j/gson/commons-lang3 canaries and both Spring Boot pins green.
- 2026-07-12: Catchable JVM linkage-error semantics (PR #1336, branch `swarm/linkage-errors`). (1) Synthetic `java/lang/LinkageError`, `java/lang/NoClassDefFoundError` (→`LinkageError`→`Error`), and `java/lang/ExceptionInInitializerError` (→`LinkageError`) are now registered in `stdlib.rs`. (2) Execution-time class-resolution failures — the `invokestatic` slow path plus `getstatic`/`putstatic`/`getfield`/`putfield` via the new `ensure_field_owner_loaded!` macro — now throw a CATCHABLE `NoClassDefFoundError` (detail message = the internal class name) routed through the real exception-table machinery (`throw_java!` / `propagate_java_exception!` / `find_exception_handler`), instead of the old fatal `Error::ClassNotFound` propagated via `?`. (3) JVMS 5.5 erroneous-`<clinit>` handling: `ClassRegistry` gained an `erroneous` HashSet; a throwing `<clinit>` moves the class initialized→erroneous; `Error`-typed clinit failures propagate unwrapped while `Exception`-typed ones wrap in `ExceptionInInitializerError`; a second use of an erroneous class throws `NoClassDefFoundError`. SIMPLIFICATION: the single-threaded init model is kept (the mark-initialized-before-`<clinit>` recursion guard is retained; no per-thread init state machine). PAYOFF: the commons-logging ladder fixture now catches the missing-`org/apache/logging/log4j/MarkerManager` LinkageError inside `Log4jApiLogFactory.<clinit>` and falls through to `LogFactoryImpl`; the ladder frontier ADVANCED from `class not found: org/apache/logging/log4j/MarkerManager` to `method not found: java/util/Hashtable.computeIfAbsent(Ljava/lang/Object;Ljava/util/function/Function;)Ljava/lang/Object;` (a collections/`NoSuchMethodError` lane). PRE-EXISTING (NOT fixed here, confirmed on baseline `ddfcfd5`): a `real-jdk-shadow` Getfield panic `heap.get(r)?.fields[fidx]` index-out-of-bounds — a field resolves to a slot beyond the allocated object layout — under `--real-jdk` against the Spring Boot fat jar; an object-layout coherence bug on the real-jdk-shadow/ClassLoader lane, matching findings-doc blocker #2 (`java/net/URL` layout).
- 2026-07-13: LADDER climb — objectId (PR #1347 invokestatic super-walk) then three in-lane rungs cleared on stacked branch `swarm/ladder-objectid` (off `swarm/invokestatic-super-walk`). The commons-logging ladder (synthetic mode) advanced objectId → `java/io/Serializable` → `Logger.logp` → Thread contextClassLoader → `Class.getInterfaces()`. FIXES (all in this lane's editable files): (1) **`java/io/Serializable`** — registered as an empty synthetic marker interface in `stdlib.rs` next to `java/lang/AutoCloseable` (`super_class: None`, empty collections, `ClassLoadSource::Synthetic`) so reflective hierarchy walks / `is_assignable_from` resolve it instead of `ClassNotFound`. Cleared `class not found: java/io/Serializable`. (2) **`java/util/logging/Logger.logp`** — commons-logging's `Jdk14Logger` routes every log call through the 4-arg `logp(Level, srcClass, srcMethod, msg)` and 5-arg `logp(..., Throwable)`; added `native_jul_logger_logp` / `native_jul_logger_logp_throwable` in `native/java_util_logging.rs` (mirror `native_jul_logger_log` / `_log_throwable`, using arg1=level, arg4=msg, arg5=thrown) + registrations in `stdlib.rs`. Cleared `method not found: java/util/logging/Logger.logp(...)`. (3) **Thread context-classloader persistence** — root cause found by temporarily instrumenting the `InvocationTargetException` wrap in `native/reflect.rs` (since reverted; reflect.rs byte-for-byte unchanged): the fixture's own reflectively-invoked `LadderApplication.main` threw `NullPointerException` because `Thread.currentThread().getContextClassLoader()` returned null. `native_thread_current_thread` allocates a THROWAWAY `java/lang/Thread` per call, so the Spring Boot launcher's `Thread.currentThread().setContextClassLoader(LaunchedClassLoader)` wrote to a discarded instance. FIX (`native/java_lang.rs` + `stdlib.rs`): persist the main thread's context loader in a new static slot `$dukeMainContextClassLoader` on the synthetic `java/lang/Thread` (declared in the Thread `ClassContext`, same singleton pattern as `$dukeSystemClassLoader`); `setContextClassLoader` (now a `register_callback` native) writes both the instance field and the static slot; `getContextClassLoader` (now `register_callback`) returns the instance field if set, else the persisted static loader, else the system loader (JVM default for the main thread). This also cleared the FOLLOW-ON rung — with the real `LaunchedClassLoader` (which carries the fat-jar archive path) restored, `getResourceAsStream("META-INF/duke-ladder.properties")` resolves `BOOT-INF/classes/...` (resource resolution keys off the loader object's `runtime_loader_paths`; a bare system loader has none). NEW LADDER FRONTIER (pinned, NOT patched — OUT OF LANE): `method not found: java/lang/Class.getInterfaces()[Ljava/lang/Class;`, which is only a SYMPTOM. `LogFactoryImpl.createLogFromClass@404` evaluates `newLogger instanceof org/apache/commons/logging/Log` on the reflectively-built `Jdk14Logger` (which really does `implements Log, Serializable`) and Duke returns FALSE: in `is_assignable_from` (`native/common.rs:11465`) a class's interface entries are plain internal names (`org/apache/commons/logging/Log`) while `to_key` is loader-qualified (`org/apache/commons/logging/Log loader:NN`), so `iface == to_key` never matches. That false negative diverts into `handleFlawedHierarchy`, which calls `getInterfaces()` and ultimately throws a spurious `LogConfigurationException` — so adding `getInterfaces` alone would be semantically WRONG (it wouldn't boot; it'd swap the error). The real fix is the interface/`to_key` key-normalization in `is_assignable_from` (`native/common.rs`) or the `Instanceof` opcode (`execution.rs`) — the interpreter class-identity/assignability lane (FORBIDDEN here), which owns the next rung. `LADDER_BLOCKER` in `duke/tests/spring_boot_real_app.rs` updated to `method not found: java/lang/Class.getInterfaces()[Ljava/lang/Class;`; ladder canary stays `#[ignore]`d. `APP_BLOCKER` untouched. Full suite 3096/0/4 (unchanged; pin-string edit only), fmt + clippy (1.97.0 pedantic+nursery) clean, HelloWorld class+jar both print `Hello, World!`. NO edits to `registry.rs` / `native/common.rs` / `execution.rs`.
- 2026-07-13: **instanceof loader-qualified-interface false-negative FIXED** (branch `swarm/instanceof-loader-keys` off trunk; cherry-picked onto `swarm/ladder-objectid`/PR #1353). This is the interpreter class-identity lane rung the previous entry pinned. ROOT CAUSE confirmed in code: in `is_assignable_from` (`native/common.rs`) a `ClassContext`'s interface entries are stored either as PLAIN internal names (classes built reflectively/synthetically via `build_class_context`, which never runs the loader-resolution pass) or as LOADER-QUALIFIED keys (`name\0loader:N`, written by `ensure_loaded_inner` at `registry.rs:1281-1292`); the target `to_key` is loader-qualified, so the old `iface == to_key` equality silently failed for the plain case. commons-logging's `LogFactoryImpl.createLogFromClass` evaluated `newLogger instanceof org/apache/commons/logging/Log` on the reflectively-built `Jdk14Logger` and got FALSE, diverting into `handleFlawedHierarchy` → `Class.getInterfaces()`. FIX (one site, ~3 lines + comment): compare interface entries on the PLAIN internal name — `class_internal_name_from_key(&iface) == to_internal` (the loader separator is `\0`; `to_internal` at line 11428 already holds the plain name). This strips the loader qualifier from both sides, is loader-agnostic per Duke's interface-by-name identity model, and fixes BOTH `Instanceof` and `Checkcast` (execution.rs:2839/2871 both route through `is_assignable_from`; execution.rs itself needed no edit). Superclass comparison left untouched (out of scope; not the diagnosed bug). REGRESSION TEST `is_assignable_from_matches_loader_qualified_interface` in `tests.rs` (plain interface entry vs loader-qualified target key = true; unrelated class = false) — FAILS on trunk, PASSES with fix. Gate: workspace 3097/0/4 (Part 1, +1 = the new test, zero instanceof/checkcast/reflection regressions), fmt + clippy 1.97.0 pedantic+nursery clean, HelloWorld class+jar both `Hello, World!`. LADDER END-TO-END: the fix clears commons-logging entirely — `Jdk14Logger.log`/`info` now execute and the ladder boots ALL THE WAY THROUGH logging into its own `LadderApplication.main`. **NEW WALL (pinned for wave 9):** an uncaught `java/lang/reflect/InvocationTargetException`. Decoded from main's bytecode (main@10-48): `new Properties().load(Thread.currentThread().getContextClassLoader().getResourceAsStream(name))` — Duke's synthetic `java/util/Properties.load(Ljava/io/InputStream;)V` throws `java/io/IOException` (cannot read the resource InputStream Duke returns), which propagates out of main and the Spring Boot launcher's reflective `main.invoke` wraps into the uncaught ITE (cause decoded via temporary instrumentation of the reflect wrap in `native/reflect.rs`, since reverted byte-for-byte). WAVE-9 LANE: java.util Properties / resource-stream I/O — teach `Properties.load(InputStream)` to actually parse the stream handed back by `getResourceAsStream`. `LADDER_BLOCKER` re-pinned to `java exception: java/lang/reflect/InvocationTargetException` (same visible string as `APP_BLOCKER`); ladder canary stays `#[ignore]`d with updated reason; comment progression updated. NO edits to `registry.rs`; Part 2 touched only `common.rs`/`tests.rs` (cherry-pick) + `spring_boot_real_app.rs` + docs.
- 2026-07-13: **LADDER climb — two more in-lane rungs cleared (`Properties.load` + character-stream stack); walls at reflective same-class private invoke** (branch `swarm/ladder-objectid`, PR #1353). The commons-logging ladder now climbs all of commons-logging/Jdk14Logger into its own `LadderApplication.main` (via the earlier PRs #1347 invokestatic super-walk / #1354 / #1355 / #1353 + the cherry-picked loader-key `instanceof` fix) and clears main's first two rungs, then walls on the third (out of lane). THIS SESSION, two in-lane rungs CLEARED: (1) **`Properties.load(InputStream)`** — `native_properties_load` (`native/java_util.rs`) threw `java/io/IOException` because `properties_stream_id_from_slot` only understood host-file-fd streams, but `getResourceAsStream` returns a synthetic `duke/io/ResourceInputStream` (backing byte-array + cursor). Added a shared `input_stream_drain_all_bytes` (in `native/java_io.rs`): detects the ResourceInputStream by class name, drains its bytes directly (advancing the cursor), else host-file drain; `properties_read_input_stream_bytes` delegates to it and feeds the existing `parse_properties_bytes`. Property values now parse and `getProperty` returns them. (2) **Character-stream stack** — main does `new BufferedReader(new InputStreamReader(getResourceAsStream("greeting.txt"), UTF_8)).readLine()`, which raised `NoSuchMethodError` on `InputStreamReader.<init>(InputStream, Charset)` (no such classes existed). Added minimal synthetic `java/io/InputStreamReader` (fields: stream, charsetName) and `java/io/BufferedReader` (fields: decoded-content String, cursor) in `stdlib.rs`, with natives in `native/java_io.rs` (`decode_bytes_with_charset` honours ISO-8859-1/US-ASCII byte-preserving vs. UTF-8 lossy; `readLine` recognises `\n`/`\r`/`\r\n` and returns null at EOF; BufferedReader eagerly drains at construction — documented simplification). CRITICAL: both readers' `super_class` is `java/lang/Object`, and Duke must NOT register a synthetic `java/io/Reader` — the OSS smoke harness runs gson against the REAL JDK-21 modules via `BootstrapLoader`, where a 0-field synthetic `java/io/Reader` shadows the real one (which declares a `lock` field) and corrupts real `StringReader`/`JsonReader` subclass field layout → `InvalidFieldref { index: 0 }` (this exact regression was caught by the gson canary during development and fixed by dropping the Reader registration). WALL — rung (3), PINNED for wave 9, OUT OF LANE: `LadderApplication.class.getDeclaredMethod("summarize", List.class).invoke(null, ...)` (main@399) invokes the class's OWN private static method via reflection without `setAccessible`. The real JVM permits this — `Method.invoke`'s access check is caller-sensitive, and a class may always reflectively access its own private/nestmate members — but Duke's `native_reflect_method_invoke` (`native/reflect.rs`) keeps the coarse `!is_public && !is_accessible → throw`, which is CORRECT for the CROSS-class case the gson canary + `ReflectionTest.privateMethodRaisesIllegalAccess` depend on (I initially relaxed this and it BROKE both — reverted). It cannot distinguish same-class from cross-class because the invoking frame's class is not available to the native: the stack snapshot in `native_control_for_call`/`native_needs_stack_snapshot` (`common.rs`, FORBIDDEN here) is captured only for Throwable-init and `Reflection.getCallerClass`, not `Method.invoke`. So the spurious `IllegalAccessException` propagates out of main and the launcher's reflective `main.invoke` wraps it into the uncaught `java/lang/reflect/InvocationTargetException` (same visible string as `APP_BLOCKER`, different cause). WAVE-9 LANE: interpreter reflection / native-boundary — add `Method.invoke`/`Constructor.newInstance` to `native_needs_stack_snapshot` so `control.stack_trace()` is populated, then allow the invoke when the caller frame's class equals the method's declaring class. Root causes for rungs (2)/(3) were decoded by temporarily adding an `eprintln!` to the reflect-wrap arm in `native/reflect.rs` (surfaced `NoSuchMethodError` → `IllegalAccessException`), since reverted byte-for-byte. KNOWN DIVERGENCE (separate lane, follow-up): `Class.getResourceAsStream("greeting.txt")` resolves the class-relative name to `com/example/duke/ladder/greeting.txt` correctly but looks it up against the BOOTSTRAP loader instead of the `LaunchedClassLoader`, so it misses `BOOT-INF/classes` and returns null (fixture logs `greeting.txt -> null` and continues). TEST/PIN: the ladder canary `spring_boot_ladder_boots_end_to_end` stays `#[ignore]`d (reason updated to the reflection wall) with a strengthened stable-substring assertion for when the wall clears; `LADDER_BLOCKER` re-pinned to the (unchanged visible) blocker and the ladder pin test kept. `APP_BLOCKER`/app pin/app canary untouched. Gate: `cargo test --workspace --no-fail-fast` = 3097 passed / 0 failed / 4 ignored (unchanged counts vs. branch baseline — new synthetics + Properties/reader natives added no new non-ignored tests), fmt + clippy 1.97.0 pedantic+nursery clean, gson/slf4j/commons-lang3 canaries + both Spring Boot pins green, HelloWorld class+jar both `Hello, World!`. NO edits to `registry.rs` / `native/common.rs` / `execution.rs`.
- 2026-07-13: Stage-c charset/StreamEncoder writer graph (branch `swarm/io-stage-c`). PROVEN end-to-end (under reverted throwaway fixes): the REAL `OutputStreamWriter -> sun.nio.cs.StreamEncoder -> Charset` writer pipeline, driven over a NON-allowlisted `OutputStream` sink (a user-defined subclass wrapping a real `ByteArrayOutputStream`, so no `FileOutputStream`/`KEEP_SYNTHETIC` gate), runs clean through `Charset.forName`/`UTF_8`/`CharsetEncoder`/`StreamEncoder.<init>` and, once its walls are crossed, encodes+writes `w.write("hello\n"); w.flush()` to exactly `[104,101,108,108,111,10]="hello\n"` (`WriterGraphProbe.RESULT=6`). FINDING: the charset floor is essentially ALREADY PRESENT — the only blockers are GENERIC interpreter gaps, not charset-specific. The full ordered 5-wall chain: (1) `getstatic java/nio/ByteBuffer.UNSAFE` where `UNSAFE` is inherited from superclass `java/nio/Buffer` [FORBIDDEN — needs superclass-walk static resolution + eager superclass `<clinit>` per JVMS 5.5]; (2) `jdk/internal/misc/ScopedMemoryAccess.registerNatives ()V`; (3) `jdk/internal/misc/Unsafe.isBigEndian ()Z`; (4) `jdk/internal/access/JavaLangAccess.encodeASCII ([CI[BII)I`; (5) `ReentrantLock` `IllegalMonitorStateException` from an unstable `Thread.currentThread()` identity [reverted; needs heap-scoped stable currentThread]. LANDED (commit): 3 additive DORMANT natives for walls #2–#4 — `ScopedMemoryAccess.registerNatives`→`native_void_noop`, `Unsafe.isBigEndian`→`native_false_boolean`, and `native_java_lang_access_encode_ascii` (mirrors `StringCoding.implEncodeAsciiArray`: copy `sa[sp+i]`→`da[dp+i]` as bytes while `<0x80`, stop at first `>=0x80`, return count; pinned by two direct-dispatch unit tests). HONEST COMMITTED-TREE WALL (guarded by `tests/writer_graph_frontier.rs`): `InvalidFieldref { index: 0 }` at `java/nio/ByteBuffer::<clinit>@16 getstatic ByteBuffer.UNSAFE` (wall #1). Also corrects the 2026-07-12 §9 "real FileOutputStream writes, exit 0" claim: that holds ONLY under the reverted throwaway probe dropping `FileOutputStream` from `KEEP_SYNTHETIC`; on the committed tree (CLI `DUKE_REAL_JDK=1` and in-test `enable_real_jdk_shadow` share `should_shadow_synthetic`/`KEEP_SYNTHETIC`) a `FileOutputStream(FileDescriptor)` sink walls at `MethodNotFound java/io/FileOutputStream.<init>(Ljava/io/FileDescriptor;)V` (guarded by `tests/streamencoder_writer_frontier.rs`). HANDOFF to `swarm/arcstr-interning` (owns `registry.rs`/`native/common.rs`/`execution.rs`): two GENERIC interpreter-linkage fixes — (A) `getstatic`/`putstatic` must walk the superclass chain (mirror instance-field `field_slot_idx`); (B) `ensure_initialized` must eagerly init the direct superclass first (JVMS 5.5); plus deferred (C) heap-scoped stable `Thread.currentThread()`. (A)+(B) also gate the `String.COMPACT_STRINGS` frontier from the 2026-07-12 properties entry. Full findings: `docs/findings/2026-07-11-system-io-real-layout-blockers.md` §10.
- 2026-07-13: Stage-c FRONTIER ADVANCED past `ByteBuffer.UNSAFE` — PR #1354 cleared wall #1; the 3 natives are now LIVE-COVERED (branch `swarm/io-stage-c`, rebased onto trunk `98a02b6`). PR #1354 ("interp: resolve inherited statics + eager superclass init, JVMS 5.4.3.2 / 5.5") landed exactly the handed-off fixes (A) superclass-walk `getstatic`/`putstatic` and (B) eager direct-superclass `<clinit>` in the forbidden files. RE-PROBED: `java/nio/ByteBuffer.<clinit>` now runs to `@29 return` (the old `InvalidFieldref { index: 0 }` wall #1 is GONE), and the writer graph flows through walls #2–#4. The 3 stage-c natives moved from DORMANT to LIVE-COVERED on the real writer path (confirmed via `DUKE_TRACE_EXEC=1` + a reverted throwaway `eprintln!`): `ScopedMemoryAccess.registerNatives` (`ScopedMemoryAccess.<clinit>@0`→`@19 return`), `Unsafe.isBigEndian` (`ByteOrder.<clinit>@27`→false→`@45 return`, `NATIVE_ORDER=LITTLE_ENDIAN`), and `JavaLangAccess.encodeASCII` (fires `sp=0 dp=0 len=6`, encodes all 6 bytes of `"hello\n"` inside `StreamEncoder.implWrite@18`). NEW HONEST COMMITTED-TREE WALL = wall #5: after the encode, `StreamEncoder.write` releases its `ReentrantLock` and `ReentrantLock$Sync.tryRelease@24 athrow`s `java/lang/IllegalMonitorStateException` (verbatim: `JavaException { class_name: "java/lang/IllegalMonitorStateException" }`) because `Thread.currentThread()` returns a fresh throwaway identity per call, so the lock's exclusive-owner check fails on release. This is the deferred wave-9 fix (C) heap-scoped stable `currentThread` — OUT OF THIS LANE. `writer_graph_frontier.rs` `EXPECTED_FRONTIER` re-pinned to the new verbatim wall (stays a frontier guard, not yet a real-write assertion — completion is gated on (C)). `streamencoder_writer_frontier.rs` (FileOutputStream FD-ctor allowlist wall) unaffected by #1354, still green. NO forbidden-file edits (registry.rs/common.rs/execution.rs) on this branch — (A)+(B) are on trunk via #1354. Full suite unchanged counts; gates clean.
- 2026-07-15: **EnumSet + enum-reflection landed; APP fixture clears the EnumSet wall** (branch `swarm/enumset-reflection`). Implemented synthetic `java/util/EnumSet` (ordinal bit-set backed, `fields[0]`=element-type `Class`, `fields[1]`=`long` bitmask, ≤64 constants: factories `of`/`noneOf`/`allOf`/`range`/`copyOf` incl. the `of(Enum, Enum[])` overload, instance `contains`/`add`/`remove`/`size`/`isEmpty`/`iterator`/`toArray`; `allOf`/`range` materialize the universe by heap-scan of instantiated constants ordered by ordinal) in `native/java_util.rs` + `stdlib.rs`, plus `Class.isEnum`/`Class.getEnumConstants` enum-constant reflection (heap-scan-by-ordinal in declaration order) in `native/reflect.rs` + `stdlib.rs`, plus 9 tests in `tests.rs` with fixtures `tests/fixtures/EnumSetTest.java`/`.class`/`EnumSetTest$Day.class` pinned to JDK21 golden. PAYOFF: the app fixture's `ConcurrentReferenceHashMap$Task.<init>` (the old EnumSet wall) now executes and `EnumSet.of(Enum,Enum[])`/`noneOf`/`contains` all work. The VISIBLE app blocker string is UNCHANGED (`java exception: java/lang/reflect/InvocationTargetException`) so the app pin `spring_boot_app_surfaces_next_missing_capability_explicitly` still holds and `APP_BLOCKER` is NOT changed — but the real underlying cause MOVED off the enum lane. NEW FRONTIER (traced via `DUKE_TRACE_EXEC`, OUT OF LANE — reference/GC lane): `java/lang/ref/SoftReference.<init>(Ljava/lang/Object;Ljava/lang/ref/ReferenceQueue;)V` is not registered (`stdlib.rs` has synthetic `Reference`/`WeakReference`/`ReferenceQueue` but no `SoftReference`), hit at `org/springframework/util/ConcurrentReferenceHashMap$SoftEntryReference.<init>@4 invokespecial`, caught+rethrown at `ConcurrentReferenceHashMap$Segment.doTask@178 athrow`. Gate: `cargo test --workspace` = 3108 passed / 0 failed / 4 ignored (enum lane added 9 tests), fmt + clippy 1.97.0 pedantic+nursery clean, both Spring Boot pins green, both canaries fail as assertions (not panics), HelloWorld class both modes print `Hello, World!`. Changes limited to `native/java_util.rs` / `native/reflect.rs` / `stdlib.rs` / `tests.rs` + new fixtures; `APP_BLOCKER` and `tests/spring_boot_real_app.rs` untouched.
- 2026-07-15: **LADDER BOOTS END-TO-END — caller-sensitive reflective access (`Method.invoke`/`Constructor.newInstance` same-class rule)** (branch `swarm/same-class-reflection`). The wave-9 wall pinned above is CLEARED and the commons-logging ladder canary `spring_boot_ladder_boots_end_to_end` is un-ignored and passing. ROOT CAUSE (confirmed exactly as the prior entry predicted): `LadderApplication.main` does `LadderApplication.class.getDeclaredMethod("summarize", List.class).invoke(null, ...)` — a same-class reflective invoke of its OWN private static method without `setAccessible`. The real JVM permits this (JLS 6.6: `Method.invoke`'s access check is caller-sensitive; a class may always reflectively access its own members). Duke's `native_reflect_method_invoke` (`native/reflect.rs`) kept the coarse `!is_public && !is_accessible → throw`, spuriously throwing `IllegalAccessException`, which the Spring Boot launcher's reflective `main.invoke` wrapped into the uncaught `java/lang/reflect/InvocationTargetException`. FIX (two files, minimal): (1) **`native_needs_stack_snapshot`** (`native/common.rs`, reflection region) now also fires for `java/lang/reflect/Method.invoke(Ljava/lang/Object;[Ljava/lang/Object;)Ljava/lang/Object;` and `java/lang/reflect/Constructor.newInstance([Ljava/lang/Object;)Ljava/lang/Object;`, so `native_control_for_call` captures the invoking Java frame — `control.stack_trace()[0]` names the caller class (same mechanism `Reflection.getCallerClass` already uses; the native is not itself in the snapshot, so `frames[0]` is its caller). (2) **`native_reflect_method_invoke`/`native_reflect_constructor_new_instance`** (`native/reflect.rs`) take `control` (no longer `_control`) and the access check becomes `!is_public && !is_accessible && !caller_is_same_class(control, declaring_class_key)`. New helper `caller_is_same_class` compares `class_internal_name_from_key(frames[0].class_name) == class_internal_name_from_key(declaring_class_key)` (both sides normalized to strip any `\0loader:N` qualifier, matching Duke's by-name class identity). `setAccessible` still bypasses exactly as before (via `is_accessible`); an empty snapshot → rejected (conservative, matches pre-fix throw). CRITICAL GUARDRAIL HELD: the check is NARROWED to same-class only — CROSS-class private invoke still throws. Verified: the gson canary (`gson_smoke_runs_real_jar_bytecode`) and `ReflectionTest.privateMethodRaisesIllegalAccess` (ReflectionTest invokes ReflectionTarget's private `hidden()` — different classes → still throws) both stay green. NEST-MATES (JEP 181, NestHost/NestMembers) are NOT modelled — `ReflectedClassInfo` carries no nest membership — so only strict same-class access is allowed; cross-nest private invoke still throws (documented in a one-line code comment). New hermetic unit test `caller_is_same_class_allows_same_class_and_rejects_cross_class` (`tests.rs`) locks in same-class-allow / cross-class-reject / loader-key-normalization / empty-snapshot-reject. The ladder now prints its full happy path (`Reflective summarize() -> LOGGING,RESOURCE-SCAN,REFLECTION,STREAMS` … `Duke ladder fixture completed all rungs successfully.` / `application started successfully.`) and exits 0. TEST CHANGES (`duke/tests/spring_boot_real_app.rs`): removed `#[ignore]` from `spring_boot_ladder_boots_end_to_end`; repurposed the obsolete next-blocker pin `spring_boot_ladder_surfaces_next_missing_capability_explicitly` into a no-regression guard `spring_boot_ladder_surfaces_no_missing_capability` (asserts clean boot + no `runtime error`), mirroring the commons-lang3 `_surfaces_no_missing_capability` precedent; `LADDER_BLOCKER` retired in favour of `LADDER_REGRESSED_MARKER = "runtime error"`. `APP_BLOCKER`/app pin/app canary untouched (the Spring Boot app fixture still walls at its EnumSet/enum-reflection frontier). Gate (all green, local): `cargo build` clean; `cargo test --workspace` = **3101 passed / 0 failed / 3 ignored** (baseline 3099/0/4; +2 passed = the new unit test + the un-ignored ladder canary; ignored 4→3); `cargo fmt --check` clean; clippy 1.97.0 pedantic+nursery clean; HelloWorld `run` (class) + `-jar` both print `Hello, World!`; OSS canaries (gson/slf4j/commons-lang3/commons-logging-ladder) all pass; Spring/real-jdk pins (`spring_boot_real_app` 3/0/1, `spring_boot_real_jdk`, `real_jdk_shadow`, `module_model`, `classloader_bootstrap_frontier`, `layout_coherence`) all pass. Files touched: `native/common.rs` (reflection region only — `native_needs_stack_snapshot`), `native/reflect.rs` (the two reflect natives + `caller_is_same_class`), `tests.rs` (unit test), `duke/tests/spring_boot_real_app.rs`, `README.md`, `MEMORY.md`, `docs/findings/2026-07-10-spring-boot-real-app.md`. NO edits to `registry.rs` / `execution.rs` / the STRING/heap/EnumSet/threading regions of `common.rs`.
