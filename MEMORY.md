# MEMORY

- 2026-04-29: Before issue #637, ServiceLoader providers whose static initialization or constructors touched `java.util.concurrent.atomic` failed before useful work. The `service-loader-atomic.jar` fixture now exercises a provider backed by `AtomicLong`; `AtomicServiceLoaderInteropTest.providerClinitUsesAtomicLong()` passes after the atomic synthetic classes and natives were added.
- 2026-04-30: Before issue #640, classes whose static initialization touched `java.util.concurrent.ConcurrentHashMap` could fail before `main()` because Duke had no CHM synthetic class or natives. `ConcurrentHashMapStaticInitTest` now covers a static cache that calls `put` and `computeIfAbsent` during `<clinit>` and then reads/prints `42 84`.
- 2026-04-30: Before issue #643, wire-protocol style static initialization that called `"text".getBytes(StandardCharsets.UTF_8)` or `Charset.forName("UTF-8")` failed because Duke had no `java.nio.charset` synthetics or `String` byte conversion natives. `CharsetClinitInteropTest` now covers a static UTF-8 header plus protocol charset lookup and prints `11` / `UTF-8`; the round-trip fixtures cover UTF-8, UTF-16, UTF-16BE, UTF-16LE, US-ASCII, and ISO-8859-1.
- 2026-04-30: Before issue #648, any class initializer caching `java.util.logging.Logger.getLogger(...)` failed before `main()` because Duke had no JUL synthetic classes. `JulClinitSurvivalTest` now covers the representative `Logger` + `AtomicLong` + `Pattern` static-init chain, and the JUL fixtures verify `INFO`/`WARNING` routing, `FINE` dropping, canonical `Level` statics/parse, logger interning, and `LogRecord` field storage.
- 2026-04-30: Before issue #649, config-heavy JARs failed on `new java.util.Properties()` because Duke had no synthetic `Properties` class. `PropertiesBasicTest` now covers ISO-8859-1 `load(InputStream)`, Java properties escapes, defaults chaining, deterministic `store(OutputStream, String)` round-trips, property-name enumeration, and local map views. `Properties` extends a minimal synthetic `Hashtable` for hierarchy compatibility, but Duke still does not model `Hashtable` synchronization.
- 2026-05-01: Before issue #658, libraries that touched richer `java.time` APIs would fall over on missing synthetic classes or identity-based semantics. `JavaTimeBasicTest` now covers `Instant`, `Duration`, `LocalDate`, and `LocalDateTime` constructors/factories, arithmetic, parse/toString round-trips, value-based `compareTo`/`equals`/`hashCode`, `Instant.now()` wall-clock interop, `Duration.between(...)`, and `DateTimeFormatter` ISO constants plus `DateTimeParseException` hierarchy wiring.
- 2026-05-01: Issue #660 added Duke's first hermetic third-party OSS JAR smoke harness under `tests/fixtures/oss-jars/`, using `slf4j-simple-2.0.13` + `slf4j-api-2.0.13` plus a `Slf4jSimpleSmoke` fixture. The harness initially discovered `java/lang/System.getSecurityManager()Ljava/lang/SecurityManager;` as the first missing capability in execution order, so the full end-to-end canary remains `#[ignore]` while the smoke advances one explicit gap at a time.
- 2026-05-12: Issue #695 added the no-security-manager `java/security/AccessController.doPrivileged(...)` slice, including `PrivilegedAction`, `PrivilegedExceptionAction`, and checked-exception wrapping through `PrivilegedActionException.getException()`. The `slf4j-simple` smoke now progresses past `System.getSecurityManager()` and `AccessController.doPrivileged(...)`; the current explicit blocker is `java/lang/Thread.getContextClassLoader()Ljava/lang/ClassLoader;`.
- 2026-06-08: Issue #854 registered `java/lang/String.equalsIgnoreCase(Ljava/lang/String;)Z` on the synthetic `java/lang/String` class, implementing Java SE 21's locale-independent two-step fold (`Character.toUpperCase` OR `Character.toLowerCase`) via `chars_equal_ignore_case` so corner cases like the KELVIN SIGN `U+212A` fold to `'k'`. Null argument returns false; differing-length strings short-circuit. New `StringEqualsIgnoreCase` fixture prints `OK`. The `slf4j-simple` smoke now progresses past `String.equalsIgnoreCase`; the current explicit blocker is `java/util/ArrayList.<init>(I)V`.
- 2026-07-10: Issue #1308 cleared the final `slf4j-simple` blocker chain and lit up the end-to-end canary. The last six explicit capabilities were added in execution order: `java/util/ArrayList.<init>(I)V`, `java/lang/StringBuilder.<init>(I)V`, `java/lang/Thread.getName()Ljava/lang/String;`, `java/lang/StringBuilder.append(Ljava/lang/CharSequence;II)Ljava/lang/StringBuilder;`, `java/lang/Class.isArray()Z`, and `java/io/PrintStream.flush()V`. `slf4j_simple_smoke_runs_real_jar_bytecode` is now un-ignored and passing: it runs unmodified `slf4j-simple` bytecode to completion, loading `org/slf4j/simple/SimpleLogger` and `org/slf4j/simple/SimpleServiceProvider` from classfiles (`ClassLoadSource::Classfile`) and emitting `INFO duke-smoke - hello world`. The now-obsolete `slf4j_simple_smoke_surfaces_next_missing_capability_explicitly` probe (which asserted an explicit next blocker via `expect_err`) was removed, since the smoke no longer returns an error.
- 2026-07-10: The `commons-lang3-3.17.0` end-to-end canary is now green. `commons_lang3_smoke_runs_real_jar_bytecode` runs unmodified `commons-lang3` bytecode to completion (`StringUtils.join`/`capitalize`, `ArrayUtils.add`/`contains`/`toObject`), emitting `commons-lang3: join=duke-jvm-smoke capitalize=Hello added=1,2,3,4 contains4=true`. The blocker chain was cleared in execution order: `java/lang/Character.toTitleCase(I)I`+`(C)C` (simple titlecase mapping; digraphs `U+01C4..U+01CC`/`U+01F1..U+01F3` map to their `Lt` forms, everything else to simple uppercase — validated against OpenJDK 21 goldens in a `tests_char_titlecase` unit test), `java/lang/Character.charCount(I)I`, `java/lang/String.<init>([III)V` (code-point-array constructor used by `StringUtils.capitalize`), `java/lang/reflect/Array.newInstance(Ljava/lang/Class;I)Ljava/lang/Object;`/`getLength(Ljava/lang/Object;)I`/`set(Ljava/lang/Object;ILjava/lang/Object;)V` (first `java.lang.reflect.Array` natives; `newInstance` maps primitive `TYPE` classes such as `Integer.TYPE` to `int[]` and `set` unboxes wrappers into primitive arrays), `java/lang/Class.getComponentType()Ljava/lang/Class;`, and `java/util/Arrays.setAll([Ljava/lang/Object;Ljava/util/function/IntFunction;)V` (element-wise lambda fill, used by `ArrayUtils.toObject`). A final rendering gap was also fixed: `java/util/Objects.toString(Object)`/`toString(Object,String)` were bypassing wrapper handling and printing `ClassName@ref`; they now delegate to `heap_object_to_string`, so boxed `Integer`s stringify as their values. The former next-blocker pin `commons_lang3_smoke_surfaces_next_missing_capability_explicitly` was repurposed into `commons_lang3_smoke_surfaces_no_missing_capability`, a regression guard that fails with the exact rendered capability if any blocker reappears.
- 2026-07-10: The `gson-2.11.0` `toJson`/`fromJson` canary (`oss_jar_smoke::gson_smoke_runs_real_jar_bytecode`) is now GREEN end-to-end: it runs unmodified `gson` bytecode to serialize `new Pojo(7, "duke")` and deserialize it back, printing `gson round-trip: {"count":7,"name":"duke"} -> count=7 name=duke`. The load-bearing root cause was a GC bug where nested `<clinit>` execution states were not registered as GC roots, so a collection during a nested class initializer freed live objects and left stale roots; fixed separately in PR #1318 / branch `swarm/gc-nested-clinit-roots` ("register all active nested execution states as GC roots"). On top of that fix, a 21-blocker capability chain was cleared in execution order: the `java/lang/reflect/Type` marker interface, the `Class` reflection natives (`getModifiers`, `isInterface`, `isPrimitive`, `isRecord`, `isAnonymousClass`, `getGenericSuperclass`, `isAssignableFrom`, `cast`), `Field.getModifiers`/`getGenericType`/`getType`/`get`, reflective `sun/misc/Unsafe.allocateInstance` for `fromJson` (gson's `ConstructorConstructor` fallback for `Pojo`), boxing all numerics (`Long`/`Short`/`Byte`/`Double`/`Float`, mirroring `Integer`) to extend `java/lang/Number` so `instanceof`/`checkcast Number` is correct, plus assorted `String`/`StringBuffer`/`Map` natives. KNOWN LIMITATIONS still open (tracked as `TODO(known-limitation)` in `native/reflect.rs`): `Class.getModifiers`/`isInterface` report ACC_PUBLIC-only (the real `ClassAccessFlags` are not carried on `ReflectedClassInfo`), and `Field.getModifiers` omits the transient/final bits (dropped when the Field mirror is built) — surfacing either needs plumbing through `registry.rs`. Additionally `sun/misc/Unsafe` and the synthetic `ThreadLocal` are not on the `KEEP_SYNTHETIC` allowlist, so an eventual `real_jdk_shadow` run would shadow them with real JDK bytecode that Duke does not yet fully support.
- 2026-07-11: System/IO real-layout phase 1 = investigation + `setOut0`/`setErr0`/`setIn0` System-init seeding scaffolding (commit `df33dcd`; shared `set_system_stream` helper + 3 static callback natives on `java/lang/System`, bootstrap routed through the same helper byte-identically, +5 tests). No `KEEP_SYNTHETIC` reduction was safely landable (stayed at 10) — full blocker map in `docs/findings/2026-07-11-system-io-real-layout-blockers.md`. KEY durable facts for future waves: (1) **bytecode wins over registered natives for shadowed classes** (`apply_shadow_override` `NativeOverride`→`Bytecode` at `execution.rs:40-46`, invokestatic path `execution.rs:542-566`, gate `registry.rs:1038-1041`), so real-layout migrations CANNOT use a native-shim shortcut — the real object graph must be genuinely well-formed. (2) Real instance-slot counts: `System` = 0 (slot-match but behavioral blocker), `PrintStream` = 8 own / 11 total (NOT "11 own"), `FileOutputStream`/`FileInputStream` = 5 each. (3) Dropping `File*Stream` is a VERIFIED REGRESSION: real `FileOutputStream.<clinit>@0` → `SharedSecrets.getJavaIOFileDescriptorAccess()` (`FD_ACCESS==null`) → `MethodHandles.Lookup.ensureInitialized` → the whole `java.lang.invoke` engine → unregistered `jdk/internal/reflect/Reflection.getClassAccessFlags(Ljava/lang/Class;)I` crash; also drags in `Blocker` (worse because Duke seeds `VM.isBooted()==true` at `jdk_internal.rs:231`) and `Cleaner`/`FileCleanable` — a Module/reflection lane (`Reflection.getClassAccessFlags` + `MethodHandles` bootstrap) is the SHARED prerequisite. (4) `System` migration is additionally gated by two tests that hard-lock it synthetic: `tests/real_jdk_shadow.rs` `KEEP_SYNTHETIC_SAMPLE` (~line 110) and `tests/layout_coherence.rs` `audit_reports_nonempty_candidates` (~line 213).
- 2026-07-11: Minimal-but-honest `java.lang.Module` model + real class/field access-flag plumbing. (1) Access flags are now threaded end to end through the reflection channel: `ReflectedClassInfo` and `ReflectedFieldInfo` carry a raw `access_flags: u16`, populated from the real classfile in `reflected_class_info_from_loader` (and reconstructed to the legacy default for synthetic-stub fallback paths). The three `TODO(known-limitation)` stubs in `native/reflect.rs` are DELETED and corrected: `Class.getModifiers` now returns the real class flags masked to the JLS set with `ACC_SUPER`/`ACC_MODULE` stripped (matching `HotSpot`'s `JVM_GetClassModifiers`), `Class.isInterface` reads the real `ACC_INTERFACE` bit, and `Field.getModifiers` reports the real field flags including `final` (0x10) / `transient` (0x80) / `volatile` (0x40) — previously public/static-only. All three became `register_callback` natives so they can read `ops.inspect_class(...)`. `ClassContext`/`FieldEntry` were intentionally NOT touched (239/272 literals) — the flags ride the `Reflected*Info` structs, which is the actual channel natives use. (2) `java.lang.Module`: every class lives in the shared unnamed module of the system class loader. New natives (in `native/java_lang.rs`, registered in `stdlib.rs`) intercept the real JDK methods #1319-style: `Class.getModule()` returns a lazily-allocated, string-interned singleton `java/lang/Module` instance (no synthetic class registered, no `KEEP_SYNTHETIC` entry needed — natives answer every call), `Module.isNamed()`→false, `Module.getName()`→null, `Module.canUse(Class)`→true; plus `jdk/internal/reflect/Reflection.getClassAccessFlags(Class)` returning the real class flags. Under `real_jdk_shadow` with a real JDK-21 jimage, the unmodified slf4j-simple smoke now flows the real `java.util.ServiceLoader` PAST the entire module-system wall — `Class.getModule` → `Module.isNamed` → `Reflection.verifyModuleAccess` → `getClassAccessFlags` → `ServiceLoader.checkCaller` returns — landing at the NEXT (separate, larger) frontier: `java/lang/ClassLoader.<clinit>` → `registerNatives()V` / the system-class-loader bootstrap (the same ClassLoader wall the spring-boot fixtures are pinned behind, out of scope here). Before this change the wall was `MethodNotFound java/lang/Class.getModule`. Proof: new in-crate unit tests exercise the four Module natives + the corrected `getModifiers`/`isInterface`/`Field.getModifiers` (transient|final|private surfaced), and `tests/module_model.rs` is a real-jdk-gated regression guard that the module wall stays cleared. The gson/commons-lang3/slf4j canaries remain green (the reflect natives still satisfy gson). Remaining KNOWN LIMITATION: `sun/misc/Unsafe` and synthetic `ThreadLocal` are still not on `KEEP_SYNTHETIC`.
- 2026-07-11: Broke the `java/lang/ClassLoader` bootstrap wall under `real_jdk_shadow` (branch `swarm/classloader-bootstrap`). Because `ClassLoader` is NOT on `KEEP_SYNTHETIC`, the real JDK-21 `ClassLoader` classfile loads and its real `<clinit>` runs; its very first instruction is a private static `registerNatives()V`, which is genuinely `native` in the JDK (no bytecode body). With no native registered, the interpreter ran off the empty method body and died with the opaque `FellOffEnd` error at `<clinit>@16` — the entire ClassLoader init walled at instruction 1. FIX: a single no-op native `java/lang/ClassLoader.registerNatives()V` (`native_class_loader_register_natives` in `native/java_lang.rs`, registered in the ClassLoader block of `stdlib.rs`; Duke wires each ClassLoader native explicitly so there is nothing to register). This is a minimal-native fix, NOT a `KEEP_SYNTHETIC` addition — the primary mission goal was to make the real `<clinit>` work, which it now does. WHAT NOW INITIALIZES: the real `ClassLoader.<clinit>` runs to completion (`@26 return`), and the real `getSystemClassLoader()` → `initSystemClassLoader()` path runs the real bytecode and returns the system loader (`getSystemClassLoader@95 areturn`). Execution then flows well past the ClassLoader lane into the real `ServiceLoader` module lookup: `ServiceLoader$ModuleServicesLookupIterator` → `jdk/internal/loader/BootLoader.<clinit>` → `SharedSecrets.getJavaLangAccess` → `jdk/internal/util/StaticProperty.<clinit>`. NEW VERBATIM FRONTIER: `MethodNotFound { name: "java/lang/System.getProperties", descriptor: "()Ljava/util/Properties;" }`. This is a SEPARATE subsystem (system-properties bootstrap), not the ClassLoader lane: `StaticProperty.<clinit>`'s first act is `System.getProperties()`, and under the shadow flag `java/util/Properties` is itself shadowed by real `Hashtable` bytecode, so satisfying it requires materializing a live, well-formed shadowed `Properties`/`Hashtable` object graph during bootstrap (recall the durable "bytecode wins over registered natives for shadowed classes" fact) — a cross-lane, non-minimal refactor, deliberately left as the honest pin. PROOF/GUARD: new non-ignored real-jdk-gated pin test `tests/classloader_bootstrap_frontier.rs::slf4j_real_jdk_shadow_classloader_frontier_pin` drives the same slf4j path and asserts this exact verbatim first-blocker; the pre-existing `module_model::slf4j_clears_module_wall_under_real_jdk_shadow` stays green (its assertions were not weakened). OBJECT-MODEL COHERENCE: the synthetic `native_class_loader_get_system_class_loader` (which caches into the `$dukeSystemClassLoader` static field) and the real `getSystemClassLoader` bytecode are mutually exclusive by mode — under the shadow flag the real bytecode wins (the synthetic ClassLoader ctx with its static field is shadowed away and never registered), so the two never diverge; nothing needed changing for coherence. UNRELATED (forbidden lane, NOT fixed, still reproduces): the real-jdk fat-jar path panics with `index out of bounds: the len is 1 but the index is 10` at `execution.rs:1614:46` (getfield) — verified still present via `DUKE_REAL_JDK=1 duke -jar tests/fixtures/oss-jars/spring-boot/duke-spring-boot-app-3.5.12.jar`. The two spring-boot synthetic pins (`spring_boot_real_app`: APP → `DateTimeFormatter.ofPattern`, LADDER → `MarkerManager`) did NOT move — they run in default synthetic mode, past the ClassLoader lane, so the registerNatives native (shadow-only) has no effect on them.
- 2026-07-12: Hashtable Map-default family + real `System.getProperties()` (branch `swarm/properties-hashtable`). TWO pins advanced. (1) LADDER (synthetic mode), registration-only: the synthetic `java/util/Hashtable` (which reuses the HashMap layout + `native_hashmap_*` natives, `stdlib.rs`) previously registered only init/put/get/containsKey/size/remove/isEmpty/keySet/values/entrySet/clear. It now registers the full Map-default family, mirroring HashMap: value-returning via `register` (`putIfAbsent`, `getOrDefault`, `replace`, `containsValue`, `putAll`) and lambda-driven via `register_callback` (`computeIfAbsent`, `computeIfPresent`, `compute`, `merge`, `forEach`, `replaceAll`) — all reusing the existing HashMap natives (no new native code). PAYOFF: the commons-logging ladder fixture's `LogFactoryImpl` fallback path clears the `Hashtable.computeIfAbsent` rung; the ladder frontier ADVANCED from `method not found: java/util/Hashtable.computeIfAbsent(Ljava/lang/Object;Ljava/util/function/Function;)Ljava/lang/Object;` to `method not found: org/apache/commons/logging/impl/LogFactoryImpl.objectId(Ljava/lang/Object;)Ljava/lang/String;` (a different missing-method rung in commons-logging's own bytecode; the collections lane is cleared). `LADDER_BLOCKER` in `duke/tests/spring_boot_real_app.rs` updated to the new frontier. (2) REAL-JDK (`DUKE_REAL_JDK=1`), new native: `System.getProperties()Ljava/util/Properties;` is now a `register_callback` native (`native_system_get_properties` in `native/java_lang.rs`, registered in the System block of `stdlib.rs`). Because under the shadow flag `java/util/Properties` is shadowed by real `Hashtable` bytecode (bytecode-wins rule), a native shim is impossible — so it materializes a GENUINE, real-layout Properties object graph, mirroring `native_reflect_constructor_new_instance`: `ops.ensure_class_initialized("java/util/Properties")` → `ops.allocate_instance("java/util/Properties")` → `ops.invoke("<init>","()V")` (runs REAL bytecode under shadow) → for each system property `ops.invoke("setProperty","(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/Object;")` with real heap Strings → return the ref. The key/value set is the `system_property_snapshot` helper, consistent with `system_property_value` (`common.rs`, the `java.version="21"` set — NOT the inconsistent `system_property_value_fallback` "1.8.0" set), plus any `setProperty` overrides. VERIFIED FULLY CONSTRUCTED (no half-migration): traced execution confirmed `ensure_class_initialized`, `allocate_instance`, real `<init>()V`, and all 9 `setProperty` invocations complete and the live Properties ref returns. The real-jdk frontier (`tests/classloader_bootstrap_frontier.rs`, `EXPECTED_FRONTIER`) ADVANCED from `MethodNotFound { name: "java/lang/System.getProperties", descriptor: "()Ljava/util/Properties;" }` to `InvalidFieldref { index: 0 }`. That new blocker is in a SEPARATE lane: `StaticProperty.<clinit>` reads the returned properties and execution proceeds until a real `getstatic java/lang/String.COMPACT_STRINGS` — a real-JDK static field that the synthetic, `KEEP_SYNTHETIC` `java/lang/String` does not declare, so `static_field_idx` fails with `InvalidFieldref { index: 0 }` (identified via temporary instrumentation, since reverted). This is the String real-layout / `KEEP_SYNTHETIC` lane, out of scope for the system-properties lane and left as the honest pin. COHERENCE: HelloWorld runs clean in synthetic and real-jdk modes; `DUKE_LAYOUT_CHECK=fail DUKE_REAL_JDK=1` HelloWorld shows no layout-check failure; the returned Properties is fully real-layout (allocate + real `<init>`), so no synthetic/real slot divergence. Full workspace suite stays 3083/0/4; clippy pedantic+nursery (1.97.0) and fmt clean; slf4j/gson/commons-lang3 canaries and both Spring Boot pins green.
- 2026-07-12: Catchable JVM linkage-error semantics (PR #1336, branch `swarm/linkage-errors`). (1) Synthetic `java/lang/LinkageError`, `java/lang/NoClassDefFoundError` (→`LinkageError`→`Error`), and `java/lang/ExceptionInInitializerError` (→`LinkageError`) are now registered in `stdlib.rs`. (2) Execution-time class-resolution failures — the `invokestatic` slow path plus `getstatic`/`putstatic`/`getfield`/`putfield` via the new `ensure_field_owner_loaded!` macro — now throw a CATCHABLE `NoClassDefFoundError` (detail message = the internal class name) routed through the real exception-table machinery (`throw_java!` / `propagate_java_exception!` / `find_exception_handler`), instead of the old fatal `Error::ClassNotFound` propagated via `?`. (3) JVMS 5.5 erroneous-`<clinit>` handling: `ClassRegistry` gained an `erroneous` HashSet; a throwing `<clinit>` moves the class initialized→erroneous; `Error`-typed clinit failures propagate unwrapped while `Exception`-typed ones wrap in `ExceptionInInitializerError`; a second use of an erroneous class throws `NoClassDefFoundError`. SIMPLIFICATION: the single-threaded init model is kept (the mark-initialized-before-`<clinit>` recursion guard is retained; no per-thread init state machine). PAYOFF: the commons-logging ladder fixture now catches the missing-`org/apache/logging/log4j/MarkerManager` LinkageError inside `Log4jApiLogFactory.<clinit>` and falls through to `LogFactoryImpl`; the ladder frontier ADVANCED from `class not found: org/apache/logging/log4j/MarkerManager` to `method not found: java/util/Hashtable.computeIfAbsent(Ljava/lang/Object;Ljava/util/function/Function;)Ljava/lang/Object;` (a collections/`NoSuchMethodError` lane). PRE-EXISTING (NOT fixed here, confirmed on baseline `ddfcfd5`): a `real-jdk-shadow` Getfield panic `heap.get(r)?.fields[fidx]` index-out-of-bounds — a field resolves to a slot beyond the allocated object layout — under `--real-jdk` against the Spring Boot fat jar; an object-layout coherence bug on the real-jdk-shadow/ClassLoader lane, matching findings-doc blocker #2 (`java/net/URL` layout).
