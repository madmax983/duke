#[test]
fn test_helpers() {
    let mut map = std::collections::HashMap::<i32, i32>::new();
    let mut set = std::collections::HashSet::<String>::new();

    // Just invoke the raw functions with dummy arguments since we can't easily make a serializer
    // Let's check what's going on.
}
