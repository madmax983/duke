⚡ Bolt: Resolve recent clippy warnings

💡 What: Addressed `clippy::unnecessary_sort_by` by replacing `.sort_by(|a, b| b.cmp(a))` with `.sort_by_key(|b| std::cmp::Reverse(b))` in `duke-telemetry/src/lib.rs` and `duke/src/jar_analyze.rs`. Also addressed `clippy::map_unwrap_or` by replacing `.map(...).unwrap_or(...)` with `.map_or(...)` in `duke-interpreter/src/native.rs`.
🎯 Why: Clippy checks on the CI failed due to these lint warnings.
📊 Impact: Zero performance impact. Code is more concise and conforms to modern idiomatic Rust.
🔬 Measurement: Run `cargo clippy --all-targets --all-features -- -D warnings`.
