--- execution.rs
+++ execution.rs
@@ -225,6 +225,7 @@
 }

 /// Main bytecode execution loop for a Java thread.
+#[allow(clippy::large_stack_frames)]
 pub fn run_execution(
     control: &mut ThreadControl,
     heap: &mut duke_gc::Heap,
