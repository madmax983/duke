[package]
name = "duke-bytecode"
version.workspace = true
edition.workspace = true
description = "JVM bytecode definitions, decoder, and structural verifier for Duke JVM"

[dependencies]
duke-classfile = { path = "../duke-classfile" }
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
